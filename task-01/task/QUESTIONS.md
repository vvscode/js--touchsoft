1.  Что такое git?
    * Репозиторий
1.  Какие есть системы контроля версий?
    * Github, Git, Subversion, Mercurial CVS, TFS
1.  как создать ветку в git?
    * git checkout <branch> (https://git-scm.com/docs/git-checkout)
1.  что такое HTML?
    * язык гипертекстовой разметки
1.  что такое CSS?
    * Cascading Style Sheets — формальный язык описания внешнего вида документа
1.  как зовут создателя JavaScript?
    * Брендан Эйх
1.  в каком году был создан JavasScript?
    * 1996
1.  Какая текущая версия Javacript?
    * 1.8.5
1.  Какая прошлая версия Javascript?
    * 1.8.2
1.  Какого номера версии Javascript не было?
    * 1.8.3, 1.8.4
1.  Как называется комитет, занимающийся развитием JavaScript?
    * Netscape
1.  Для чего нужен файл .gitignore?
    * игнорировать файлы
1.  Как добавить в git пустую папку?
    * В нужной папке создается пустой (0 байт) файл с именем .gitkeep. Затем добавить его
1.  Что такое "линтер"?
    * инструмент проверки качества программного кода на JavaScript
1.  Для чего нужен "prettier"?
    * форматирование кода
1.  Как открыть DevTools в браузере?
    * f12
1.  Как можно добавить javascript на страницу?
    * \<script\>
1.  Сколько будет 0.2 + 0.1?
    * 0.30000000000000004
1.  Что такое breakpoint?
    * используется во время отладки
1.  Какие методы есть у объекта console?
    * error, info, log, warn
1.  На странице два элемента с id="awesomeId". Как из javascript найти второй?
    * никак
1.  Что такое falsy (ложное) значение? Сколько их?
    * if (false)
    * if (null)
    * if (undefined)
    * if (0)
    * if (NaN)
    * if ('')
    * if ("")
    * if (document.all) [1]
1.  Назовите 4 javascript фреймворка
    * angular
    * vue
    * ember
    * mocha
1.  Назовите 3 javascript библиотеки
    * jQuery
    * Underscore 
    * React
1.  Что такое cookies?
    * небольшой фрагмент данных, отправляемый сервером на браузер пользователя
1.  Что происходит между вводом в адресной строке адреса страницы и появлением страницы на экране?
    * клиент -> запрос -> вебсервер -> вебприложение -> вебсервер -> клиент
1.  Зачем нужен DNS сервер?
    *  DNS связывает доменные имена с цифровыми адресами
1.  Что такое HTTP?
    * протокол передачи данных
1.  Назовите 3 программы-веб сервера
    * Apache
    * NGINX
    * Microsoft IIS
1.  Назовите 5 программ-браузеров
    * google chrome
    * firefox
    * yandex
    * opera
    * safari
1.  Полное название Javascript?
    * Javascript
1.  Что такое "транспиляция" в контексте разработки на Javascript?
    * преобразование кода js в промежуточный код
1.  Как в Slack оформить многострочный код?
    * \`\`\` (нужный язык)
        * console.log('Hello World');
    * \`\`\` 
1.  Как в Slack оформить однострочный код?
    *  \`console.log('Hello World');\`
1.  Что такое "императивное программирование"?
    * Императи́вное программи́рование — это парадигма программирования (стиль написания исходного кода компьютерной программы), для которой характерно следующее:
        * в исходном коде программы записываются инструкции (команды);
     инструкции должны выполняться последовательно;
     при выполнении инструкции данные, полученные при выполнении предыдущих инструкций, могут читаться из памяти;
     данные, полученные при выполнении инструкции, могут записываться в память.
1.  Что такое ООП?
    * методология программирования, основанная на представлении программы в виде совокупности объектов, каждый из которых является экземпляром определённого класса, а классы образуют иерархию наследования
1.  Что такое переменная?
    * поименованная, либо адресуемая иным способом область памяти, адрес которой можно использовать для осуществления доступа к данным и изменять значение в ходе выполнения программы.
1.  Как объявить переменную? Как инициализировать переменную? Примеры кода
    * var a;
    * a = 3;
1.  Виды циклов ( 3 вида )? Примеры использования
    * while(true){console.log('Hello');};
    * for(; ;){console.log('Hello');};
    * do{}while(true){console.log('Hello');};
1.  Сколько и какие типы данных в javascript? Примеры создания переменных каждого типа
    * (number) var n = 1;
    * (string) var str = 'as';
    * (boolean) var b = true;
    * (null) var n = null;
    * (undefined) var n;
    * (object) var o = {name:'Petya'};
1.  Как узнать тип переменной? Пример кода
    * var a = 3; typeof(a);
1.  Что такое область видимости?
    * кусок кода где есть доступ к переменной
1.  Как из javascript в брaузере вывести на экран строчку? ( 4-ре способа ) Примеры кода
    * window.alert("sometext");
    * window.confirm("sometext");
    * window.prompt("sometext","defaultText");
    * alert("sometext");
1.  Каким образом можно реализовать условное выполнение кода?
    * сделать разветвление
1.  Условные операторы в javascript - какие? Примеры использования
    * if(true){var a = 3;}
    * if(b > 3){b = 4;}else{b = 2;}
    * return b < 3 ? true : false;
1.  Что такое тернарный оператор?
    * ?:
1.  Как обойтись без условных операторов если нужно сделать условное выполнение кода? С примером
    * * return b < 3 ? true : false;
1.  Что такое функция? Какими характеристиками можно описать функцию?
    * специальный тип объектов, позволяющий формализовать средствами языка определённую логику поведения и обработки данных.
1.  Как можно создать функцию? 3 способа. C примерами кода
    * function a(){};
    * var a = function(){};
    * (function(){});
1.  Что такое объект? Как создать объект? ( 3 способа ) С примерами кода
    * o = new O();
    * O.prototype = Object.create(A.prototype);
    * var o = {name: 'sd'};
1.  Что такое передача по ссылке / передача по значению?
    * передача переменноли либо по ссылке либо по значению
1.  Что такое минификация и зачем она нужна?
    * процесс, направленный на уменьшение размера исходного кода путём удаления ненужных символов без изменения его функциональности.
1.  Как можно добавить javascript код в html страницу? 2 способа. C примерами кода
    * document.head.appendChild(document.createElement('script').setAttribute('src','test.js'));
    * <script type="text/javascript" src="test.js"></script>
1.  Что такое массив?
    *  структура данных в виде набора компонентов, расположенных в памяти непосредственно друг за другом,
1.  Как создать массив? ( 3 способа ) С примером кода
    * var a = [];
    * var a = new Array();
    * var a = 'a,b'.split(',');
1.  Как запросить данные у пользователя? с примером кода
    * window.confirm("sometext");
    * window.prompt("sometext","defaultText");
1.  Что такое замыкание?
    * функция внутри функции
1.  Как узнать сколько параметров принимает функция?
    * arguments.length
1.  Как узнать есть ли у объекта свойство x ?
    * if(bla in obj){};
1.  Как сделать ревес строки?
    * 'asdasdasd'.split('').reverse().join("");
1.  Как удалить предпоследний элемент в массиве?
    * delete array[array.length - 1]
1.  Как заменить в строке "papa roach" все буквы a на A ?
    * "papa roach".replace(new RegExp('a', 'g'), 'A')
1.  Сколькими способами ( с примерами кода ) можно найти на странице div с id someId ?
    * document.getElementById('someId');
    * document.querySelector('#someId')
1.  Что такое API? Примеры?
    * программный интерфейс приложения
    * https://id.twitch.tv/oauth2/authorize?client_id=\<your client ID>
        &redirect_uri=\<your registered redirect URI>
        &response_type=\<type\>
        &scope=\<space-separated list of scopes\>
1.  Как в js реализовать многопоточность?
    * никак
1.  Сколькими способами можно проитерироваться по всем полям объекта? С примерами кода
    * for (var bla in obj){}
    * Object.keys(obj).forEach();
1.  Что такое callback ? Зачем? Пример кода
    * фу́нкция обра́тного вы́зова 
    * setTimeout(function(){console.log('Hello');}, 100);
1.  Почему 1 + '2' ? и 1 - '2' ?
    * неявное приведение типов
1.  Как и когда работает преобразование типов?
    * https://learn.javascript.ru/types-conversion
1.  Что такое HTTP ? Из чего состоит HTTP протокол?
    * Протокол передачи данных
    * строка запроса,   заголовки, тело сообщения
1.  Какие есть методы HTTP запросов? Для чего каждый?
    * GET - Используется для запроса содержимого указанного ресурса
    * HEAD - как get но нет тела
    * POST - Применяется для передачи пользовательских данных заданному ресурсу.
    * PUT - рименяется для загрузки содержимого запроса на указанный в запросе URI
    * PATCH - Аналогично PUT, но применяется только к фрагменту ресурса.
    * DELETE - Удаляет указанный ресурс. 
    * TRACE - Возвращает полученный запрос так, что клиент может увидеть, какую информацию промежуточные серверы добавляют или изменяют в запросе. 
    * CONNECT - Преобразует соединение запроса в прозрачный TCP/IP-туннель
1.  Что такое REST ? RPC ?
    *  архитектурный стиль взаимодействия компонентов распределённого приложения в сети.
    * Удаленный вызов процедур
1.  Что такое "линтер" (linter)? Зачем они нужны? Какие линтеры есть для javascript?
    * инструмент проверки качества программного кода на JavaScript
    * JSLint, JSHint, ESlint
1.  Что такое "jsdoc" ? Зачем он нужен? Примеры тегов
    * генератор документации в HTML-формате из комментариев исходного кода на JavaScript
    * Нужен для генарции документации по коду
    * @author, @constructor, @deprecated, @param...
1.  Что такое пакетный менеджер? Какие пакетные менеджеры есть для js?
    * это комплекс программного обеспечения, которое выполняет установку, настройку, удаление, а также обновление отдельных пакетов
    * npm, bower
1.  Что делают системы сборки? Какие системы сборки есть для js?
    * это инструмент, который делает сборку проекта
    * gulp, grunt 
